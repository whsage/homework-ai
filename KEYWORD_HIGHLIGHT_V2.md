# 关键词高亮优化 - 更符合做题需求

## ✅ 优化完成

重新设计了关键词高亮逻辑，现在更符合学生做题时的实际需求！

## 🎯 **优化目标**

### 之前的问题
- ❌ 高亮了太多普通词语（如"使用"、"代入"）
- ❌ 没有突出真正重要的判断关键词
- ❌ 专有名词识别不够精准
- ❌ 缺少物理、化学等学科专有名词

### 现在的改进
- ✅ **优先高亮判断类关键词**（正确/错误、能/不能等）
- ✅ **突出专有名词**（电功率、电流、电压等）
- ✅ **识别数值单位**（8W、1.5欧等）
- ✅ **保留操作指令**（计算、求、解等）

## 📊 **关键词分类（按优先级）**

### 🔴 **优先级 1：判断类关键词**（最重要！）
这些词决定了题目的答案方向：

```
正确、错误、对、错、是、否
能、不能、可以、不可以
应该、不应该、会、不会
符合、不符合、满足、不满足
超过、不超过、大于、小于、等于
最大、最小、最多、最少、至少、至多
```

**示例**：
- "判断下列说法是否**正确**"
- "电流**能**通过导体"
- "功率**不超过**额定功率"

### 🔵 **优先级 2：物理/化学专有名词**

#### 电学
```
电功率、电流、电压、电阻、电能、电功
串联、并联、短路、断路、电路
欧姆定律、焦耳定律
```

#### 力学
```
速度、加速度、力、重力、摩擦力、压强
功、功率、机械效率、动能、势能
```

#### 热学
```
温度、热量、比热容、内能
```

#### 光学
```
折射、反射、光速、波长、频率
```

**示例**：
- "**电功率**公式：P = I²R"
- "**串联**电路中**电流**处处相等"

### 🟢 **优先级 3：数学专有名词**

#### 几何
```
面积、周长、体积、表面积、半径、直径
角度、三角形、正方形、长方形、圆、扇形
```

#### 代数
```
方程、不等式、函数、解、根、系数
平方、立方、开方、因式分解
```

#### 统计
```
平均数、中位数、众数、概率、百分比
```

**示例**：
- "求**圆**的**面积**"
- "解**方程**得 x = 5"

### 🟡 **优先级 4：操作指令词**
```
计算、求、解、证明、判断、比较、选择
代入、化简、整理、推导、验证
画出、标出、写出、列出、说明
```

**示例**：
- "**计算**电流的大小"
- "**代入**公式求解"

### 🟣 **优先级 5：逻辑连接词**
```
因为、所以、如果、那么、则
由于、因此、故、可得
首先、其次、然后、最后、接着
```

**示例**：
- "**因为** P = 8瓦，**所以**..."
- "**首先**计算电阻"

### 🟠 **优先级 6：重点提示词**
```
注意、关键、重点、核心、要点
特别、尤其、务必、千万、切记
```

**示例**：
- "**注意**单位换算"
- "**关键**在于理解公式"

## 🔢 **数值单位高亮**

### 支持的单位
```
长度：千米、公里、米、厘米、毫米、km、m、cm、mm
速度：千米/小时、米/秒、km/h、m/s
质量：千克、公斤、克、kg、g
时间：小时、分钟、秒、天、年、月、周、h、min、s
货币：元、角、分
面积：平方米、m²
体积：立方米、m³
电学：瓦、千瓦、安、伏、欧、焦、W、kW、A、V、Ω、J
力学：牛、帕、N、Pa
温度：℃、°C
```

### 示例
- "**8W**" - 8瓦
- "**1.5欧**" - 1.5欧姆
- "**120千米/小时**" - 120千米每小时
- "**252千米**" - 252千米

## 📝 **实际效果对比**

### 示例 1：电学题目

**原文**：
```
在串联电路中，电流处于相等。
使用电功率公式：P = I²R，其中 P = 8瓦，R = 1.5欧。
将已知代入公式，解方程求出电流 I。
计算结果后，与选项对比，选择最接近的答案。
```

**之前的高亮** ❌：
```
在**串联**电路中，**电流**处于相等。
**使用****电功率****公式**：P = I²R，其中 P = **8瓦**，R = **1.5欧**。
将已知**代入****公式**，**解****方程****求**出**电流** I。
**计算****结果**后，与**选项****对比**，**选择**最接近的**答案**。
```
- 问题：太多普通词被高亮，干扰阅读

**现在的高亮** ✅：
```
在**串联**电路中，**电流**处于相等。
使用**电功率**公式：P = I²R，其中 P = **8瓦**，R = **1.5欧**。
将已知**代入**公式，**解****方程****求**出**电流** I。
**计算**结果后，与选项**对比**，**选择**最接近的答案。
```
- 改进：只高亮关键专有名词和操作词

### 示例 2：判断题

**原文**：
```
判断下列说法是否正确：
1. 电流能通过导体
2. 功率不能超过额定功率
3. 串联电路中电压相等
```

**现在的高亮** ✅：
```
**判断**下列说法**是否****正确**：
1. **电流****能**通过导体
2. **功率****不能****超过**额定**功率**
3. **串联**电路中**电压**相等
```
- 重点：**正确**、**能**、**不能**、**超过** 等判断词被突出

### 示例 3：选择题

**原文**：
```
小明从13:00到15:00行驶了252千米，
最高限速120千米/小时，判断是否超速。
```

**现在的高亮** ✅：
```
小明从13:00到15:00行驶了**252千米**，
**最高**限速**120千米/小时**，**判断****是否****超速**。
```
- 重点：数值单位、**是否**、**超速** 等关键词

## 🔧 **技术实现**

### 核心逻辑
```javascript
// 1. 定义关键词（按优先级分类）
const keywords = {
    judgement: ['正确', '错误', '能', '不能', ...],
    physics: ['电功率', '电流', '电压', ...],
    math: ['面积', '周长', '方程', ...],
    operations: ['计算', '求', '解', ...],
    logic: ['因为', '所以', '如果', ...],
    emphasis: ['注意', '关键', '重点', ...]
};

// 2. 按优先级合并
const allKeywords = [
    ...keywords.judgement,   // 最高优先级
    ...keywords.physics,
    ...keywords.math,
    ...keywords.operations,
    ...keywords.logic,
    ...keywords.emphasis
];

// 3. 按长度排序（避免"电功率"被"功率"替换）
allKeywords.sort((a, b) => b.length - a.length);

// 4. 高亮处理
uniqueKeywords.forEach(keyword => {
    const regex = new RegExp(`(?<!\\*)(${keyword})(?!\\*)`, 'g');
    text = text.replace(regex, '**$1**');
});
```

### 防止重复高亮
```javascript
// 使用负向前瞻和后顾
(?<!\\*)  // 前面不是 *
(keyword) // 关键词
(?!\\*)   // 后面不是 *

// 示例：
"**电功率**" 不会被再次替换为 "****电功率****"
```

## 📚 **使用场景**

### 1. 物理题
```
在**串联**电路中，**电流**处处相等。
**使用****电功率**公式 P = I²R。
**判断****是否****符合**欧姆定律。
```

### 2. 数学题
```
**求****圆**的**面积**。
**解****方程** 2x + 3 = 7。
**计算****平均数**。
```

### 3. 判断题
```
下列说法**是否****正确**？
电流**能****不能**通过绝缘体？
功率**超过****不超过**额定值？
```

### 4. 选择题
```
**选择****最大**的数。
**比较**两个数的**大小**。
**判断**哪个选项**符合**条件。
```

## 🎯 **优化效果**

### 阅读体验
- ✅ 一眼看到关键判断词
- ✅ 快速识别专有名词
- ✅ 清晰的数值单位
- ✅ 不被无关词干扰

### 做题效率
- ✅ 快速找到题目要求（正确/错误）
- ✅ 准确识别物理量（电功率、电流）
- ✅ 注意数值范围（最大、最小）
- ✅ 理解逻辑关系（因为、所以）

## 🎉 **总结**

通过重新设计关键词分类和优先级：

✅ **判断词优先** - 正确/错误、能/不能  
✅ **专有名词突出** - 电功率、电流、电压  
✅ **数值单位清晰** - 8W、1.5欧、120km/h  
✅ **操作指令明确** - 计算、求、解  
✅ **逻辑关系清楚** - 因为、所以、如果  

从"高亮过多"到"精准高亮"，让做题更高效！🎯
